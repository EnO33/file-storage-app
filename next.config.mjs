/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "wooden-salmon-890.convex.cloud"
            },
        ]
    }
};

export default nextConfig;
